/**
 * Invoice print handler for APEX modal page 120.
 * Paste into Page > Execute when Page Loads  OR  link as Static File.
 *
 * Usage: the Print button's Dynamic Action calls  invgPrintInvoice();
 */
function invgPrintInvoice() {
  window.print();
}
